# forge build cache / solc input-output dumps (regenerated by `forge build`); the compiled
# per-contract artifacts under test-artifacts/*.s.sol/ ARE committed (the test reads them).
test-artifacts/build-info/
cache/
