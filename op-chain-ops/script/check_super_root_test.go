package script

import (
	"testing"
)

func TestCalculateSuperRoot(t *testing.T) {
	t.Run("Handles when input empty", func(t *testing.T) {
		t.Skip("Test empty chains case needs implementation")
	})

	t.Run("Handles finding an anchortimestamp and super root for a single chain", func(t *testing.T) {
		t.Skip("Test single chain case needs implementation")
	})

	t.Run("Handles finding an anchortimestamp and super root for multiple chains", func(t *testing.T) {
		t.Skip("Test multiple chains case needs implementation")
	})

	t.Run("ChainIds Sorted Correctly", func(t *testing.T) {
		t.Skip("Test chain id sorting needs implementation")
	})

}
