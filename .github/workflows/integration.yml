name: Integration Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable

      - name: Download op-reth
        run: |
          ./scripts/download-op-reth.sh
          echo "$PWD/op-reth" >> $GITHUB_PATH

      - name: Build
        run: cargo build

      - name: Log Op-reth version
        run: |
          op-reth --version
          ./target/debug/rollup-boost --version

      - name: Run tests
        run: cargo test --features integration -- integration::integration_test::tests
