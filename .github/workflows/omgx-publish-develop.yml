name: Publish Packages (omgx-develop)

on:
  push:
    branches:
      - 'develop'
# on:
#   push:
#     branches:
#       - 'master'
#       - 'develop'
#       - '*rc'
#       - 'regenesis/*'
#   pull_request:
#   workflow_dispatch:

jobs:
  build-publish-win:
    runs-on: ubuntu-latest
    env:
        DOCKER_BUILDKIT: 1
        COMPOSE_DOCKER_CLI_BUILD: 1
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - uses: actions/checkout@v2

      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - uses: actions/cache@v2
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Build the services
        working-directory: ./ops
        run: |
          ./scripts/build-ci.sh
          docker build ../ --file docker/Dockerfile.omgx_monorepo --tag omgx/omgx_builder:latest
          docker build ../ --file docker/Dockerfile.omgx_deployer --tag omgx/omgx_deployer:latest

      - name: Rename and retag the optimism images
        working-directory: ./ops
        run: |
          docker image tag ethereumoptimism/builder omgx/builder:latest
          docker image tag ethereumoptimism/hardhat omgx/hardhat:latest
          docker image tag ethereumoptimism/deployer omgx/deployer:latest
          docker image tag ethereumoptimism/data-transport-layer omgx/data-transport-layer:latest
          docker image tag ethereumoptimism/l2geth omgx/l2geth:latest
          docker image tag ethereumoptimism/message-relayer omgx/message-relayer:latest
          docker image tag ethereumoptimism/batch-submitter omgx/batch-submitter:latest
          docker image tag ethereumoptimism/integration-tests omgx/integration-tests:latest

          docker image tag omgx/omgx_builder:latest omgx/omgx_builder:develop
          docker image tag omgx/omgx_deployer:latest omgx/omgx_deployer:develop
          docker image tag ethereumoptimism/builder omgx/builder:develop
          docker image tag ethereumoptimism/hardhat omgx/hardhat:develop
          docker image tag ethereumoptimism/deployer omgx/deployer:develop
          docker image tag ethereumoptimism/data-transport-layer omgx/data-transport-layer:develop
          docker image tag ethereumoptimism/l2geth omgx/l2geth:develop
          docker image tag ethereumoptimism/message-relayer omgx/message-relayer:develop
          docker image tag ethereumoptimism/batch-submitter omgx/batch-submitter:develop
          docker image tag ethereumoptimism/integration-tests omgx/integration-tests:develop
          docker rmi $(docker images | grep 'ethereumoptimism/' | awk '{print $1}')
          docker images

      - name: Rename and retag the optimism images
        working-directory: ./ops
        run: |
          for i in $(docker images --format "{{.Repository}}:{{.Tag}}" | grep omgx); do
            echo "$1"
            docker push "$i"
          done
