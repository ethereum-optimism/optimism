name: Sysgo E2E Tests
on:
  push:
    branches: [main]
  merge_group:
  pull_request:
env:
  CARGO_TERM_COLOR: always
jobs:
  node-e2e-sysgo-tests:
    runs-on: kona-8cores
    timeout-minutes: 60
    name: ${{ matrix.devnet-config }}-sysgo-tests
    strategy:
      fail-fast: false
      matrix:
        devnet-config: ["simple-kona", "simple-kona-geth", "simple-kona-sequencer", "large-kona-sequencer"]
    steps:
      - name: Checkout sources
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          large-packages: false

      - uses: ./.github/actions/setup
        with:
          channel: stable
          components: llvm-tools-preview

      - uses: taiki-e/install-action@just

      - uses: taiki-e/install-action@cargo-llvm-cov

      - uses: jdx/mise-action@v3       # installs Mise + runs `mise install`

      - name: Setup Go 1.23.8
        uses: actions/setup-go@v5
        with:
          # Semantic version range syntax or exact version of Go
          go-version: '1.23.8'
          cache-dependency-path: |
            tests/go.sum
            tests/optimism/go.sum

      - name: run common tests for node with sysgo orchestrator and report coverage
        run: |
          echo "Building op-reth..."
          just build-reth
          echo "Unzipping contract artifacts..."
          just unzip-contract-artifacts
          echo "Building kona-node with llvm-cov..."
          source <(cargo llvm-cov show-env --export-prefix)
          just build-kona
          echo "Running tests..."
          just test-e2e-sysgo-run node node/common "${{ matrix.devnet-config }}"
          cargo llvm-cov report --lcov --output-path node_common_cov.lcov

      - name: Upload coverage to codecov.io
        uses: codecov/codecov-action@v5
        with:
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
          files: node_common_cov.lcov
          flags: e2e
          env_vars: OS,RUST
          name: node-sysgo-tests-common
          verbose: true
      
      - name: reorg tests for node with sysgo orchestrator and report coverage
        run: |
          echo "Building kona-node with llvm-cov..."
          source <(cargo llvm-cov show-env --export-prefix)
          just build-kona
          just test-e2e-sysgo-run node node/reorgs "${{ matrix.devnet-config }}"
          cargo llvm-cov report --lcov --output-path node_reorgs_cov.lcov

      - name: Upload coverage to codecov.io
        uses: codecov/codecov-action@v5
        with:
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
          files: node_reorgs_cov.lcov
          flags: e2e
          env_vars: OS,RUST
          name: node-sysgo-tests-reorgs
          verbose: true

  node-restart-sysgo-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    name: sysgo-restart-tests
    steps:
      - name: Checkout sources
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          large-packages: false

      - uses: ./.github/actions/setup
        with:
          channel: stable
          components: llvm-tools-preview

      - uses: taiki-e/install-action@just

      - uses: taiki-e/install-action@cargo-llvm-cov

      - uses: jdx/mise-action@v3       # installs Mise + runs `mise install`

      - name: Setup Go 1.23.8
        uses: actions/setup-go@v5
        with:
          # Semantic version range syntax or exact version of Go
          go-version: '1.23.8'
          cache-dependency-path: |
            tests/go.sum
            tests/optimism/go.sum

      - name: restart tests for node with sysgo orchestrator and report coverage
        run: |
          echo "Building op-reth..."
          just build-reth
          echo "Unzipping contract artifacts..."
          just unzip-contract-artifacts
          echo "Building kona-node with llvm-cov..."
          source <(cargo llvm-cov show-env --export-prefix)
          just build-kona
          echo "Running tests..."
          just test-e2e-sysgo node node/restart 
          cargo llvm-cov report --lcov --output-path node_restart_cov.lcov

      - name: Upload coverage to codecov.io
        uses: codecov/codecov-action@v5
        with:
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
          files: node_restart_cov.lcov
          flags: e2e
          env_vars: OS,RUST
          name: node-sysgo-tests-restart
          verbose: true




