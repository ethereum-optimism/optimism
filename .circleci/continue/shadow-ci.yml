# AUTO-GENERATED by ops/shadow-ci/cmd/generate-ci — DO NOT EDIT.
# Regenerate: go run ./ops/shadow-ci/cmd/generate-ci --config ops/shadow-ci/config --output .circleci/continue/shadow-ci.yml
#
# Shadow CI — adaptive test placement engine.
# Configure in ops/shadow-ci/config/scoping.yaml, then regenerate this file.
# The shadow-ci-tests job verifies this file is not stale.

version: 2.1

parameters:
  c-default_docker_image:
    type: string
    default: cimg/base:2026.03
  c-go-cache-version:
    type: string
    default: "v0.0"

jobs:
  shadow-ci-setup:
    docker:
      - image: << pipeline.parameters.c-default_docker_image >>
    resource_class: large
    steps:
      - checkout
      - restore_cache:
          keys:
            - shadow-ci-mise-v1-{{ checksum "mise.toml" }}
      - run:
          name: Install tools (cache miss only)
          command: |
            [ -f "$HOME/.local/bin/mise" ] || curl -sSf https://mise.run | sh
            echo 'export PATH=$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH' >> $BASH_ENV
            source $BASH_ENV
            if [ -d "$HOME/.local/share/mise/installs" ] && [ -n "$(ls -A $HOME/.local/share/mise/installs 2>/dev/null)" ]; then
              echo "Tools restored from cache — reshimming"
              mise reshim
              exit 0
            fi
            mise install go forge cast anvil just make gotestsum golangci-lint mockery
      - save_cache:
          key: shadow-ci-mise-v1-{{ checksum "mise.toml" }}
          paths:
            - ~/.local/share/mise/installs
            - ~/.local/bin
      - run:
          name: Build shadow CI binaries
          command: |
            [ -f "$HOME/.local/bin/mise" ] || curl -sSf https://mise.run | sh
            export PATH=$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH
            mise reshim
            cd ops/shadow-ci
            mkdir -p bin
            CGO_ENABLED=0 go build -o bin/affected ./cmd/affected
            CGO_ENABLED=0 go build -o bin/flake-reactor ./cmd/flake-reactor

            CGO_ENABLED=0 go build -o bin/compare ./cmd/compare
            CGO_ENABLED=0 go build -o bin/aggregate ./cmd/aggregate
            CGO_ENABLED=0 go build -o bin/validate ./cmd/validate
            CGO_ENABLED=0 go build -o bin/optimize ./cmd/optimize
            CGO_ENABLED=0 go build -o bin/auto-revert ./cmd/auto-revert
            CGO_ENABLED=0 go build -o bin/coherence ./cmd/coherence
            CGO_ENABLED=0 go build -o bin/execute ./cmd/execute
            CGO_ENABLED=0 go build -o bin/import-history ./cmd/import-history
            CGO_ENABLED=0 go build -o bin/generate-ci ./cmd/generate-ci
      - run:
          name: Update flake state
          command: |
            ops/shadow-ci/bin/flake-reactor \
              --config ops/shadow-ci/config \
              --events-dir /tmp/shadow-ci/events \
              --branch "$CIRCLE_BRANCH"
      - run:
          name: Compute pipeline decision
          command: |
            ops/shadow-ci/bin/affected \
              --config ops/shadow-ci/config \
              --repo . \
              --branch "$CIRCLE_BRANCH" \
              --events-dir /tmp/shadow-ci/events \
              --pipeline-id "$CIRCLE_PIPELINE_ID"
      - run:
          name: Stage artifacts for workspace
          command: |
            mkdir -p /tmp/shadow-ci-workspace
            cp -r ops/shadow-ci/bin /tmp/shadow-ci-workspace/bin
            cp /tmp/shadow-ci/decision.json /tmp/shadow-ci-workspace/decision.json
            cp /tmp/shadow-ci/affected.json /tmp/shadow-ci-workspace/affected.json
      - persist_to_workspace:
          root: /tmp/shadow-ci-workspace
          paths:
            - bin
            - decision.json
            - affected.json
      - store_artifacts:
          path: /tmp/shadow-ci/decision.json
          destination: shadow-ci/decision.json
      - store_artifacts:
          path: /tmp/shadow-ci/affected.json
          destination: shadow-ci/affected.json

  shadow-ci-verify:
    docker:
      - image: << pipeline.parameters.c-default_docker_image >>
    resource_class: medium
    steps:
      - attach_workspace:
          at: /tmp/shadow-ci-workspace
      - run:
          name: Verify decision coherence with mainline CI
          command: |
            /tmp/shadow-ci-workspace/bin/coherence \
              --decision /tmp/shadow-ci-workspace/decision.json
      - store_artifacts:
          path: /tmp/shadow-ci-workspace/decision.json
          destination: shadow-ci/decision.json

  shadow-ci-tests:
    docker:
      - image: << pipeline.parameters.c-default_docker_image >>
    resource_class: large
    steps:
      - checkout
      - restore_cache:
          keys:
            - shadow-ci-mise-v1-{{ checksum "mise.toml" }}
      - run:
          name: Install Go (cache miss only)
          command: |
            if [ -f /tmp/shadow-ci-tools/.cached ]; then
              echo 'export PATH=/tmp/shadow-ci-tools:$PATH' >> $BASH_ENV
              exit 0
            fi
            curl -sSf https://mise.run | sh
            echo 'export PATH=$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH' >> $BASH_ENV
            source $BASH_ENV
            mise install go
      - run:
          name: Check shadow-ci.yml is not stale
          command: |
            cd ops/shadow-ci
            go run ./cmd/generate-ci \
              --config config \
              --check ../../.circleci/continue/shadow-ci.yml
      - run:
          name: Run shadow CI test suite
          command: |
            cd ops/shadow-ci
            go test -v -count=1 ./...
      - store_test_results:
          path: /tmp/shadow-ci-test-results

  shadow-ci-build:
    docker:
      - image: << pipeline.parameters.c-default_docker_image >>
    resource_class: 2xlarge
    steps:
      - checkout
      - attach_workspace:
          at: /tmp/shadow-ci-workspace
      - restore_cache:
          keys:
            - shadow-ci-mise-v1-{{ checksum "mise.toml" }}
      - restore_cache:
          keys:
            - shadow-ci-cat-cannon_prestate-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-contracts_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_submodule_build-v1-
      - run:
          name: Install toolchain (build)
          command: |
            [ -f "$HOME/.local/bin/mise" ] || curl -sSf https://mise.run | sh
            echo 'export PATH=$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH' >> $BASH_ENV
            source $BASH_ENV
            mise reshim
            mise install rust just
            sudo apt-get update -qq && sudo apt-get install -y -qq libclang-dev
      - run:
          name: Execute build categories
          no_output_timeout: 45m
          command: |
            /tmp/shadow-ci-workspace/bin/execute \
              --group build \
              --decision /tmp/shadow-ci-workspace/decision.json \
              --config ops/shadow-ci/config \
              --results-dir /tmp/shadow-ci-test-results \
              --cache-dir /tmp/shadow-ci-cache
      - save_cache:
          when: always
          key: shadow-ci-cat-cannon_prestate-v1-{{ epoch }}
          paths:
            - /tmp/shadow-ci-cache/cannon_prestate
      - save_cache:
          when: always
          key: shadow-ci-cat-contracts_build-v1-{{ epoch }}
          paths:
            - /tmp/shadow-ci-cache/contracts_build
      - save_cache:
          when: always
          key: shadow-ci-cat-rust_build-v1-{{ epoch }}
          paths:
            - /tmp/shadow-ci-cache/rust_build
      - save_cache:
          when: always
          key: shadow-ci-cat-rust_submodule_build-v1-{{ epoch }}
          paths:
            - /tmp/shadow-ci-cache/rust_submodule_build
      - store_test_results:
          path: /tmp/shadow-ci-test-results
      - store_artifacts:
          path: /tmp/shadow-ci-test-results
          destination: shadow-ci/build

  shadow-ci-go:
    docker:
      - image: << pipeline.parameters.c-default_docker_image >>
    resource_class: 2xlarge
    circleci_ip_ranges: true
    steps:
      - checkout
      - attach_workspace:
          at: /tmp/shadow-ci-workspace
      - restore_cache:
          keys:
            - shadow-ci-mise-v1-{{ checksum "mise.toml" }}
      - restore_cache:
          keys:
            - shadow-ci-cat-cannon_prestate-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-contracts_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_submodule_build-v1-
      - run:
          name: Install toolchain (go)
          command: |
            [ -f "$HOME/.local/bin/mise" ] || curl -sSf https://mise.run | sh
            echo 'export PATH=$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH' >> $BASH_ENV
            source $BASH_ENV
            mise reshim
      - run:
          name: Execute go categories
          no_output_timeout: 45m
          command: |
            /tmp/shadow-ci-workspace/bin/execute \
              --group go \
              --decision /tmp/shadow-ci-workspace/decision.json \
              --config ops/shadow-ci/config \
              --results-dir /tmp/shadow-ci-test-results \
              --cache-dir /tmp/shadow-ci-cache
      - store_test_results:
          path: /tmp/shadow-ci-test-results
      - store_artifacts:
          path: /tmp/shadow-ci-test-results
          destination: shadow-ci/go

  shadow-ci-sol:
    docker:
      - image: << pipeline.parameters.c-default_docker_image >>
    resource_class: 2xlarge
    circleci_ip_ranges: true
    steps:
      - checkout
      - attach_workspace:
          at: /tmp/shadow-ci-workspace
      - restore_cache:
          keys:
            - shadow-ci-mise-v1-{{ checksum "mise.toml" }}
      - restore_cache:
          keys:
            - shadow-ci-cat-cannon_prestate-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-contracts_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_submodule_build-v1-
      - run:
          name: Install toolchain (sol)
          command: |
            [ -f "$HOME/.local/bin/mise" ] || curl -sSf https://mise.run | sh
            echo 'export PATH=$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH' >> $BASH_ENV
            source $BASH_ENV
            mise reshim
      - run:
          name: Execute sol categories
          no_output_timeout: 45m
          command: |
            /tmp/shadow-ci-workspace/bin/execute \
              --group sol \
              --decision /tmp/shadow-ci-workspace/decision.json \
              --config ops/shadow-ci/config \
              --results-dir /tmp/shadow-ci-test-results \
              --cache-dir /tmp/shadow-ci-cache
      - store_test_results:
          path: /tmp/shadow-ci-test-results
      - store_artifacts:
          path: /tmp/shadow-ci-test-results
          destination: shadow-ci/sol

  shadow-ci-rust:
    docker:
      - image: << pipeline.parameters.c-default_docker_image >>
    resource_class: 2xlarge
    steps:
      - checkout
      - attach_workspace:
          at: /tmp/shadow-ci-workspace
      - restore_cache:
          keys:
            - shadow-ci-mise-v1-{{ checksum "mise.toml" }}
      - restore_cache:
          keys:
            - shadow-ci-cat-cannon_prestate-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-contracts_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_submodule_build-v1-
      - run:
          name: Install toolchain (rust)
          command: |
            [ -f "$HOME/.local/bin/mise" ] || curl -sSf https://mise.run | sh
            echo 'export PATH=$HOME/.local/bin:$HOME/.local/share/mise/shims:$PATH' >> $BASH_ENV
            source $BASH_ENV
            mise reshim
            mise install rust just
            sudo apt-get update -qq && sudo apt-get install -y -qq libclang-dev
            curl -L --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/cargo-bins/cargo-binstall/main/install-from-binstall-release.sh | bash
            cargo binstall --no-confirm --no-symlinks cargo-nextest || cargo install --locked cargo-nextest
      - run:
          name: Execute rust categories
          no_output_timeout: 45m
          command: |
            /tmp/shadow-ci-workspace/bin/execute \
              --group rust \
              --decision /tmp/shadow-ci-workspace/decision.json \
              --config ops/shadow-ci/config \
              --results-dir /tmp/shadow-ci-test-results \
              --cache-dir /tmp/shadow-ci-cache
      - store_test_results:
          path: /tmp/shadow-ci-test-results
      - store_artifacts:
          path: /tmp/shadow-ci-test-results
          destination: shadow-ci/rust

  shadow-ci-misc:
    docker:
      - image: << pipeline.parameters.c-default_docker_image >>
    resource_class: medium
    steps:
      - checkout
      - attach_workspace:
          at: /tmp/shadow-ci-workspace
      - restore_cache:
          keys:
            - shadow-ci-mise-v1-{{ checksum "mise.toml" }}
      - restore_cache:
          keys:
            - shadow-ci-cat-cannon_prestate-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-contracts_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_build-v1-
      - restore_cache:
          keys:
            - shadow-ci-cat-rust_submodule_build-v1-
      - run:
          name: Install toolchain (misc)
          command: |
            sudo apt-get update -qq && sudo apt-get install -y -qq shellcheck python3-pip pipx
            pipx install semgrep || pip3 install semgrep || true
            echo 'export PATH=$PATH:$HOME/.local/bin' >> $BASH_ENV
      - run:
          name: Execute misc categories
          no_output_timeout: 45m
          command: |
            /tmp/shadow-ci-workspace/bin/execute \
              --group misc \
              --decision /tmp/shadow-ci-workspace/decision.json \
              --config ops/shadow-ci/config \
              --results-dir /tmp/shadow-ci-test-results \
              --cache-dir /tmp/shadow-ci-cache
      - store_test_results:
          path: /tmp/shadow-ci-test-results
      - store_artifacts:
          path: /tmp/shadow-ci-test-results
          destination: shadow-ci/misc

workflows:
  shadow-ci:
    jobs:
      - shadow-ci-setup:
          context:
            - shadow-ci-test-context
      - shadow-ci-verify:
          requires:
            - shadow-ci-setup
      - shadow-ci-tests
      - shadow-ci-build:
          context:
            - circleci-repo-readonly-authenticated-github-token
          requires:
            - shadow-ci-setup
      - shadow-ci-go:
          context:
            - circleci-repo-readonly-authenticated-github-token
          requires:
            - shadow-ci-setup
            - shadow-ci-build
      - shadow-ci-sol:
          context:
            - circleci-repo-readonly-authenticated-github-token
          requires:
            - shadow-ci-setup
            - shadow-ci-build
      - shadow-ci-rust:
          context:
            - circleci-repo-readonly-authenticated-github-token
          requires:
            - shadow-ci-setup
            - shadow-ci-build
      - shadow-ci-misc:
          context:
            - circleci-repo-readonly-authenticated-github-token
          requires:
            - shadow-ci-setup
