version: 2.1

jobs:
  hello-world-job:
    docker:
      - image: circleci/python:3.8  
    steps:
      - run:
          name: Say Hello
          command: echo "Hello, World!"

workflows:
  version: 2
  hello-world-workflow:
    jobs:
      - hello-world-job
