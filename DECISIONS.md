# DECISIONS

Append-only log of questions and architect rulings for the Dafny invariant assertion helpers feature.
Only the architect appends entries; never rewrite or delete past entries.
Format: `## Qn (date) — question` followed by `Ruling:`.
