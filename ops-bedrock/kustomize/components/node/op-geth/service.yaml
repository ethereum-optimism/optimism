apiVersion: v1
kind: Service
metadata:
  name: op-geth
  labels:
    app: op-geth
spec:
  clusterIP: None
  selector:
    app: op-geth
  ports:
    - port: 8545
      name: rpc
    - port: 8546
      name: ws
    - port: 8551
      name: engine