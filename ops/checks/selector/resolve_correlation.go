package selector

// Deleted: correlationCandidates — replaced by applyCorrelationSignal
// in resolve_scope.go. CI-history correlation no longer creates
// candidates post-cutover (dataflow is authoritative for selection);
// correlation only adjusts signal on already-selected candidates.
