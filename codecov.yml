codecov:
  require_ci_to_pass: false
comment:
  layout: "diff, flags, files"
  behavior: default
  require_changes: true
  flags:
    - op-contracts-tests

ignore:
  - "op-e2e"
  - "**/*.t.sol"
  - "op-contracts/test/**/*.sol"
  - "op-contracts/scripts/**/*.sol"
  - "op-contracts/contracts/vendor/WETH9.sol"
  - 'op-contracts/contracts/EAS/**/*.sol'
coverage:
  status:
    patch:
      default:
        target: auto  # target coverage is equal to the PR base
        threshold: 0% # coverage is not allowed to reduce vs. the PR base
        base: auto
        informational: true
        enabled: true
    project:
      default:
        informational: true
flag_management:
  # Note: flags should have the same name as the circleci job in which they
  # are uploaded.
  individual_flags:
    - name: op-contracts-tests
      paths:
        - op-contracts/src
      statuses:
        - type: patch
          target: 100%
    - name: bedrock-go-tests
    - name: contracts-tests
    - name: sdk-tests
